import React from 'react';
import { login } from '../Helper/Retriever';

const Login : React.FC = () => (
    <>
        esto es un puto login,
        <button type="button" onClick={login}>Click aqui anda puta zorra</button>
    </>
);

export default Login;
